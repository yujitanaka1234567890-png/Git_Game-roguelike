# 次元ローグライク（仮）開発ルール

ターン制ローグライク × モンスター収集 のWebゲーム。ユーザーはプログラミング初心者で、Claudeがコードを書き、ユーザーがテスト・フィードバックする Vibe Coding スタイル。

## 権利・オリジナリティ（最重要）
- 既存作品（特定の市販ローグライク等）の固有名詞・アイテム名・モンスター名・台詞・画像・音・独自用語は一切使わない。名前はすべてオリジナルで作る。
- コード・コメント・ドキュメント・画面表示にも既存作品名を書かない（「〜と同じ」「〜風」等の言及も不可）。
- ジャンル共通の一般的な仕組み（ターン制、グリッド移動、ランダムダンジョン、レベル等）は使ってよいが、特定作品に固有の仕組みを丸写ししない。新しい仕組みを入れる時、特定作品固有に見えるものはユーザーに確認する。
- 素材（画像・音）は利用規約を確認したものだけを使い、出典を記録する。

## 技術方針
- HTML5 + CSS + 素のJavaScript（Canvas描画）。フレームワーク・ビルドツール・npm は使わない。
- `index.html` をダブルクリックで動くことを維持する（ES Modules は file:// で動かないため、`<script>` タグ＋グローバル `Game` 名前空間で分割）。
- 1ファイル1役割。ファイルが大きくなったら分割する（目安300行）。新ファイルは index.html の読み込み順に追加。
- ルール（ロジック）と描画（renderer）を混ぜない。
- 各ステップ完了時に `docs/roadmap.md` を更新する。

## 構成
- `js/config.js` 設定値・8方向定義・階段定義(Game.STAIRS)・レア度表・乱数ヘルパー（randInt, pick）
- `js/data/` **図鑑データ**：`monsters.js`（Game.MONSTERS）/ `skills.js`（Game.SKILLS）/ `dungeons.js`（Game.DUNGEONS・Game.currentDungeon()）/ `breeding.js`（Game.BREEDING・Game.breedResult）/ `worlds.js`（Game.WORLDS・Game.DIMENSION_RULES）。種類を増やす時はここに書き足し、`admin/bestiary.html`（管理者用図鑑・設定チェック）で確認する
- `js/dungeon.js` ランダムダンジョン生成（3×3区画・全域木＋追加通路） / `js/map.js` 現在のマップ（ダンジョン or 拠点 loadStatic）・通行判定（canStep＝斜めの角すり抜け禁止）
- `js/fov.js` 視界・探索済み管理（拠点では revealAll）
- `js/mind.js` 精神力（同じ階に長居すると減り0でダンジョンに取り込まれる）
- `js/dimension.js` 次元変換（別の世界で拾ったアイテムの効き目の読み替え。表は data/worlds.js の DIMENSION_RULES）
- `js/rescue.js` 救出（はぐれた仲間の記録・紫の枠の中に立つはぐれた仲間・救出隊の派遣と結果・拠点の掲示板）
- `js/log.js` メッセージログ（新しい行が上・最大300行・技名クリックで説明 meta.skill） / `js/dialog.js` 共通の選択ウィンドウ（入れ替え確認・拠点の会話・収納箱・門・結果表示） / `js/fx.js` 見た目だけのエフェクト（光）
- `js/combat.js` 命中・ダメージ計算・ダメージ記録（applyDamage）・攻撃相手選択 / `js/leveling.js` レベル・経験値（主人公・仲間共通）
- `js/items.js` アイテム種類・床アイテム・使用/投げ効果 / `js/inventory.js` 持ち物と持ち物メニュー / `js/throw.js` 投げる（軌道・アニメ・着地）
- `js/player.js` 主人公 / `js/enemy.js` 敵（types＝Game.MONSTERS、レア度倍率 statsOf、ダンジョン別出現 pickType）・AI・壁抜け移動・経験値分配 / `js/specials.js` 技（敵は予告→溜め→発動、仲間は即時）とブレス
- `js/pathfind.js` 移動の共通処理（isOccupied/isFree、8方向BFS stepToward、canReach、freeTilesNear）
- `js/allies.js` 仲間（仲間化判定・入れ替え確認・AI・一覧表示）/ `js/squad.js` 分隊（O キー。人型を隊長に最大4体、任せた部屋の中で独立行動）
- `js/savecode.js` 記録の呪文（セーブ用パスワード：JIGEN1-検出番号-中身。拠点の記録の石碑で書き出し・地面に書く。成功ログ「地面に書いた文字が浮かび上がり光った。記録された世界線へ移動した」）
- `js/base.js` 拠点のデータ（牧場・倉庫・連れて行く/持って行く選択・踏破記録 cleared・交配 breed・発見済み discovered・localStorage保存 key=dimension-roguelike-save-v1）
- `js/basemap.js` 拠点の空間（固定マップ・牧場を歩く仲間・会話・収納箱・掲示板＝仲間選び/救出・交配小屋・門でのダンジョン選択）/ `js/bestiary.js` プレイヤー用モンスター図鑑（出会った種類 base.seen だけ詳しく載る）
- `js/data/sprites.js` ドット絵（アイテム・設備。12×12 の文字で手描き・オリジナル）/ `js/data/characters.js` 主人公・モンスターのドット絵（16×16・右向き。主人公は紙芝居用に hero_idle1/2・walk1/2・attack・hurt）と重ねる小物 / `js/data/char_frames.js` モンスターの歩き・攻撃の絵（元の絵をずらし＋行の描き直しで作る。名前_walk・名前_attack）/ `js/pixelart.js` ドット絵の描画（壁・床は模様をその場で描く）/ `js/sound.js` 効果音（Web Audio API で合成）/ `js/music.js` BGM（合成で演奏するオリジナル曲。今はボス戦のみ。refresh で update）
- `js/renderer.js` 描画（2D） / `js/render3d.js` 3D表示（試作・WebGL直書き・外部ライブラリなし。3 キーで切替、renderer.draw の先頭で分岐。ルールには触れない）＋ `js/render3d_parts.js`（アトラス・四角形の組み立て）/ `js/anim3d.js` 紙芝居の動き / `js/minimap.js` 3D時の左上の全体マップ / `js/input.js` キー入力 / `js/main.js` 状態遷移・ターン進行（endTurn→afterAction）・ダッシュ・脱出・死亡処理
- `docs/roadmap.md` 開発ステップ / `docs/design_notes.md` 仕様面の課題メモ

## 確定した仕様
- 状態（Game.state）：base / playing / menu / aim / animating / gameover。選択ウィンドウ（Game.dialog）が開いている間は、どの状態でもウィンドウ操作が優先（↑↓・Enter/スペース・Esc）。
- 移動は8方向。斜めは Shift＋矢印2つ同時押し。角の壁はすり抜け不可（敵の攻撃も同様。ただし壁抜けする敵がからむ攻撃は隣接なら届く）。
- Shift＋矢印1つ＝ダッシュ（1歩＝1ターン、敵も動く、25ms間隔）。Shiftを離す／前が壁／前に敵／隣に敵／新たな敵が視界に入る／被ダメージ／階段／アイテム／ウィンドウ表示 で停止。ダッシュでは攻撃しない。斜めとの判別は config.diagonalWait（120ms）待ち。
- 攻撃・移動・足踏み・壁への移動（＝足踏み扱い）・アイテム使用/置く/投げる で1ターン。ターン後処理は必ず Game.endTurn()→Game.afterAction()。
- 階段は「その階段の方向キーを2回」で使う（下り＝↓↓、将来の上り＝↑↑）。1回目は確認表示のみ、他方向キーで取り消し、キーリピートは無視。HPは持ち越し。敵数 = ダンジョンの enemyBase + (階-1)、最大10。
- 視界（fov.js）：自分の周囲1マスは常に見える＋部屋（入口含む）にいればその部屋全体と周囲の壁。探索済みは暗く表示、敵は視界内のみ表示。敵が主人公に気づく判定も同じ canSee。
- 敵AI：必殺技の溜め→隣接攻撃（時々必殺技の溜め開始）→（赤龍）ブレス→見えたら追跡→見失ったら最後に見た位置へ→うろつき（80%同じ部屋内、20%別の部屋）。
- 敵の種類・出現：図鑑データ参照（admin/bestiary.html）。6系統×3段階（ぬめ・トゲ・刃・龍・人形・つむじ鳥）＋交配種10＝28種。人形系は humanoid（将来の救出隊候補）。ダンジョンごとに spawns（from〜to階）で出現を決め、出やすさはレア度。3段階目は強いダンジョン（深淵の迷宮）にだけ出す。刃系は壁抜け（phasingSense 6 以内なら壁を無視して直進）、龍系はブレス（一直線2〜7マス先の最初のキャラに攻撃力×0.8、2ターンに1度）。
- レア度（config.rarities）：仲間化率 1よく見る30% / 2少しめずらしい20% / 3めずらしい7% / 4とてもめずらしい5% / 5幻3%。出現しやすさ spawn、強さと経験値の倍率 mul（1.0/1.3/1.7/2.2/3.0）も同表。敵・仲間の実際の能力値は Game.enemies.statsOf(type)。
- 技（specials.js・data/skills.js）：モンスターは複数の技を持つ（進化で増える／強い技に変わる）。敵：攻撃できる相手がいる時 specialChance(20%) で届く技から1つ選んで溜め開始→予兆をログ（⚠、log-warn）＋マップに黄枠と「!残りターン」→ specialWindup(2)ターン後に発動（届かなければ空振り）。主人公・仲間が受ける合計ダメージは最大HPの specialMaxRatio(60%) まで（即死防止）。仲間：allySkillChance(20%) で溜め開始、敵と同じく予兆をログ（◆、log-ally）＋マップに青枠、2ターン動かず溜めてから発動（階移動で溜め取りやめ）。範囲 shape：single/around/sight。
- アイテム：踏むと自動で拾う（最大12個）。I で持ち物メニュー、Enter 使う、T 投げる、D 置く、Esc/I 閉じる。1階に3〜5個。
- ダンジョン生成：部屋はセル内の左上1マス・右下2マスの余白で置き、部屋間は壁3マス以上。通路の曲がり角は部屋の壁の列/行を避ける。
- 精神力（mind.js・config.mind）：最大100。ダンジョンでは drainEvery(8) ターンごとに1ずつ減る。1つの階に「長居の猶予」を超えて居ると lingerDrainEvery(2) ターンごとに1減る。猶予＝graceTurns(165)×その階の床のマス数÷refFloorTiles(320)（広い階ほど長い、最低60）。0になると毎ターン zeroDamage(2) のダメージ（HPが尽きれば倒れる）。50%・25%・10%・0%で警告。50%を切った時に遠吠え（howl）＋ささやき（whisper「ひそひそ」）、その後も半分未満の間は howlEvery(45) ターンごとに遠吠え。30%未満で画面が紫ににじむ。回復：階段+10、澄心の香+50、煙草7、六面パズル10、リボルバートイ20/14/8/4。冒険開始時は満タン。
- 敵の湧き直し（enemies.tryRespawn）：毎ターン respawnChance(3%) で、主人公から見えず6マス以上離れた部屋の床に1体湧く（その階の上限 maxEnemies=10 まで）。
- 拠点（basemap.js）：起動時・帰還時は拠点を歩く。左が牧場（牧場の仲間が歩き回る。ぶつかる＝話しかける→「連れて行く/やめる」ウィンドウ）、右が家（収納箱▣にぶつかる→倉庫から持って行く道具を選ぶ）、下が門∩（乗る→出発確認）。帰還時は結果をウィンドウとログで表示。
- 牧場は種類のみ保存・最大30、倉庫は最大60。出発時に仲間最大7体・持ち物最大12個。仲間は毎回Lv1（statsOf の値）で作り直す。
- ダンジョン：はじまりの箱庭（10階・最初から）→ 踏破で 深淵の迷宮（15階）解放（unlockedBy）。門で行き先を選ぶ。脱出：各ダンジョン最深部の脱出口「O」で↓↓（踏破記録）、または帰還の鈴。生還時：新入り→牧場、持ち物→倉庫、冒険中の進化を牧場に確定。死亡・R2回（あきらめる）：新入りと持ち物を失い、進化も取り消し。拠点から連れてきた子（fromBase）は倒れても・入れ替えで外しても牧場に残る（元の姿で）。
- 仲間満員で心を開いた時：pending に積み、afterAction で入れ替え確認ウィンドウ（dialog）。拠点の子は「拠点へ帰る」、新入りは「お別れ」。
- 仲間：主人公側（主人公・仲間・アイテム）が敵を倒すとレア度に応じた確率で仲間化（HP半分、最大 config.maxAllies=7）。ターン順は 主人公→仲間→敵。仲間AI：隣の敵を攻撃→主人公から allyLeash 以内の見える敵へ→主人公についていく。主人公が仲間に向かって動くと位置入れ替え。階移動で全員同行。
- 攻撃相手の選択（敵・仲間・必殺技・ブレス共通、combat.chooseTarget）：候補のうちHP最少。randomTargetChance（20%）でランダム。
- 投げる：最大10マス直進、最初に当たったキャラに throwEffect（回復系＝相手回復〔敵も〕、剛力の実＝相手の攻撃力+1、それ以外＝2ダメージ）。外れたら壁手前の床（2マス以内の空き）に落ちる。
- レベル：Lv n に必要な累計 = expBase×(n-1)×n、上限999。主人公はレベルアップで最大HP+4・攻撃力+1。仲間も同じ曲線で成長（上昇量は種類ごとの growth）。**拠点に戻ると主人公も仲間もLv1に戻る**。
- 進化：各系統3段階（例 ぬめりん→ぬめろん→ぬめ大王。一覧は admin/bestiary.html）。2段階目はLv5、3段階目はLv30（はじまりの箱庭では実質届かない）。進化で別の種類になり、能力は新種類の statsOf＋growth×(Lv-1)＋アイテム等の上昇分、HP全回復、技も新種類のもの。**生きて帰った時に確定**（escapeDungeon で牧場の type を書き換え）、途中で倒れたら取り消し。描画はマス左上の点で段階表示。
- 交配（base.breed・basemap 交配小屋♥）：牧場の2体を選び、data/breeding.js の組み合わせ表（順不同）にあれば子が生まれる。親は牧場からいなくなる（ダンジョンへ帰る）。子は親より少し強い別種類（breedOnly）で固有の技を持つ。親は常に初期値なので結果は固定。未発見の子は「？？？」表示（discovered）。
- 経験値の分配：敵ごとに dmgLog を記録し、倒した時にダメージ割合で分配（四捨五入・最低1）。倒れた仲間の取り分は消滅。記録なしなら主人公が全取り。

- アイテムは1個ずつ「アイテムデータ」{type, uses, origin} で持つ（持ち物・床・倉庫とも同じ形。items.makeEntry）。uses＝使用回数（リボルバートイ用）、origin＝拾った世界。アイテム名は全世界共通。商標名は使わない（例：六面パズル）。
- 世界（data/worlds.js）：魔法の世界・深海調査・サイバーパンク・SF・スチームパンク。各ダンジョンは world を持ち（はじまりの箱庭＝魔法、深淵の迷宮＝深海調査）、壁・床の色が変わる。別の世界で拾ったアイテムを使うと DIMENSION_RULES（item, from, to, powerMul, note）で効き目を読み替え（今は空。辻褄は後で決める）。
- 救出（rescue.js）：倒れる/あきらめると、その冒険の新入り（仲間になった時の姿 origType）が「はぐれた仲間」として base.lost に記録（ダンジョン・階、最大 lostMax=5）。同じダンジョンの同じ階に着くと、その子がモンスターの姿のまま紫の枠の中に動かず立っていて、乗ると仲間に戻る（新入り扱い・満員なら入れ替え確認）。拠点の掲示板（掲）から人型（humanoid）の仲間を最大3体派遣でき、成功率＝base0.25＋隊員ごと(0.12＋段階0.08×(段階-1)＋レア度0.02×レア度)−0.02×(階-1)−ダンジョンの rescueDifficulty（5〜95%）。派遣中（ranch の onMission）は冒険・交配に使えず牧場にもいない。次の冒険から戻った時（生還でも死亡でも）に結果：成功→牧場へ、失敗→はぐれた仲間はダンジョンに帰る（仲間にならなかった扱い）。隊員は必ず戻る。
- 素材：外部の画像・音声素材は使わない。ドット絵は sprites.js に手描き（色は a=基本色/b=影/c=光＋固定色）、モンスター・アイテムは sprite/overlay で指定。効果音は sound.js の recipes に波形の組み合わせで追加。V でドット絵⇔文字、M で効果音オン⇔オフ（設定は localStorage）。
- 救出隊：最大4体、人型（humanoid）1体以上必須。人型以外の上乗せは perNonHumanoid。一度自力救出した子（rescued）はその冒険で再び倒れると記録されずダンジョンへ帰る（リリース）。
- アイテム効果は、DIMENSION_RULES で否定（読み替え）しない限り、どの世界でも同じ。
- Git：roguelike_game/ がリポジトリのルート（ブランチ main）。push・リモート作成・GitHub Pages の設定は必ずユーザー承認を取ってから。
- 画面配置：左にステータス（階・Lv・HP・精神・攻撃力・ターン・ボスHP）、その下に仲間一覧とログ。中央にゲーム画面、右に操作説明と凡例。狭い画面では縦並び。
- 持ち物は効果の分類（items の group：体力回復→精神回復→強化→攻撃→道具）で自動で並び替え、見出しを表示。
- 精神回復：澄心の香50・煙草7・六面パズル10・リボルバートイ20/14/8/4（弾倉を回す音）。回復ログは「精神力が N 回復した。」。
- はぐれた仲間は助けないまま lostDives(3) 回冒険に出るとダンジョンへ帰る（rescue.countDive/expireOld）。
- ダンジョン生成：分岐点だけの区画は必ず2本以上の通路につなぐ（removeDeadEnds）→ 通路の行き止まりなし。最下層（dungeon.boss があれば）は広いボス部屋（柱4本・脱出口・ボス）。ボスを倒すまで脱出口は封印、ボス部屋では湧き直しなし、ボスは仲間にならない。
- ダッシュ：仲間は入れ替えて進む。新しく通路（分かれ道・出入口）が隣に現れた時／部屋の出入口に着いた時に止まる。被弾しても止まらないが次の1歩まで hitPauseMs(350ms)。押しっぱなし移動も被弾直後は hitPauseMs 受け付けない。
- 敵の範囲技（around/sight）は他の敵も巻き込む（事故）。敵が敵を倒すと「敵の経験値」＝倒された敵の exp を得て、図鑑データの enemyEvoExp（敵として進化に必要な経験値。無ければ exp×enemyEvoFactor）に届くと進化（赤龍250＝ぬめりん5×50体、ぬめりんは赤龍1体でぬめ大王）。一覧は管理者図鑑の「敵としての進化」表。
- 攻撃を受けたキャラは攻撃と逆向きにのけぞり、攻撃された側に赤いとげとげ（fx.hitMark）。
- 効果音：攻撃「バシュ」・撃破「ザンッ」・回復「しゅぴんっ」・最大値/攻撃力アップ「ぐぐぐっ」・レベルアップは歪ませたのこぎり波のエレキギター風パワーコード「ジャジャーン」（主人公のみ）・階段「ざっざっざっ」・ゲームオーバーはバッハ「トッカータとフーガ ニ短調」冒頭（著作権切れ）を合成で演奏。ボス戦BGM（music.js）：ボスが生きている階にいる間ループ（テンポ160・Am→F→G→E のオリジナル曲）、ボス撃破・階移動・拠点・倒れる・効果音オフで止まる。
- 3D表示（試作）：斜め見下ろしカメラ（camHeight 10.5・camBack 7.7 の引き）が主人公の表示位置を追う。左上に薄い全体マップ。キャラの板は1.3マス（ボス2）。紙芝居（anim3d.js）：1マス110msですべる、左右で向きを変える時は紙を裏返すように細くなる、攻撃は fx.attacks（hitMark で記録）で踏み込み、やられは fx.tiltOf、主人公は動作ごとの絵、モンスターは伸び縮み・跳ね・赤み。描き直しは約30回/秒。壁は箱・床は板（模様は pixelart.js を流用）、キャラ・アイテム・設備はドット絵の板をカメラ側へ lean(25°) 傾けて立てる（紙芝居）。画面に映る範囲だけ組み立て（1回約1ms）。倒れた画面は2Dで出す。
- 拠点（22×12）：左に牧場、右の家の奥の壁ぞいに 収納箱C・掲示板K・交配小屋H・図鑑Z・記録の石碑S が2マスおき、下に門G。掲示板で「連れて行く仲間を選ぶ」「はぐれた仲間の救出」。牧場の仲間に話しかけて選ぶこともできる。
- プレイヤー用図鑑：敵として視界に入った・仲間にした（進化含む）・牧場にいる・交配で生まれた種類が載る（base.seen、セーブに含む）。未遭遇は「？？？」。詳細は絵・能力・技・特徴・進化先・見つかる場所。
- セーブ：ブラウザに自動記録＋「記録の呪文」（打ち間違い検出つき、別PCでも続きから）。
- 攻撃の見た目（fx.js）：同じターンの攻撃は gapMs(200ms) ずつずらして順番に見せる（踏み込み→impactMs(90ms)後にのけぞり・とげとげ）。攻撃の効果音も当たる瞬間に合わせて遅らせる（sound.play の combatSounds・fx.soundDelay）。計算はその場で終わっている。外れた攻撃も踏み込む（fx.swing）。
- 技の範囲 range：使い手から range マス以内（チェビシェフ距離）で見えている全員。つむじ鳥系：かまいたち2マス→嵐のかまいたち3マス→大竜巻4マス。
- 仲間は最大7体。分隊（squad.js・O キー）：主人公と同じ部屋にいる仲間から、人型1体以上・最大4体で作る。任された部屋の中だけで動き、部屋の敵を探して倒す（部屋からは出ない。敵がいなければ見張り）。O で呼び戻し、階を移ると自動で合流。分隊の子が倒れるとログに【分隊N】付きの赤字。3D・全体マップでは橙色。
- 入れ替え確認（仲間が満員の時）：新しい子の能力・技と、今の仲間それぞれのレア度・HP・攻撃力・防御力を表示。
- 倒れた冒険のログは base.lastLog（localStorage の別キー、記録の呪文には入れない）に残し、拠点のログの下に区切り線つきで表示。次の冒険に出ると消える。
- はじまりの箱庭：style "timeGarden"（pixelart.styledTile）＝時空を思わせる鈍い斑の壁（まだら＋青緑のにじみ＋星の点）と継ぎ目のある床（まれに時の粒が光る）。4×4マスで模様がつながる。ダンジョンの colors は世界の色より優先。
- 3D表示の操作：＋／－（マウスホイールも）でカメラを寄せる・引く（zoom 0.55〜1.6、保存）。N で全体マップの大きさ（非表示・小・中・大、保存）。モンスターも歩き・攻撃の絵に切り替わる。
